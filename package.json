{
  "name": "nodejs-express-tdd",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "jest --coverage --verbose",
    "test:unit:controllers": "jest --coverage --verbose ./tests/controllers",
    "test:unit:middlewares": "jest --coverage --verbose ./tests/middlewares",
    "test:unit:models": "jest --coverage --verbose ./tests/models",
    "test:unit:repositories": "jest --coverage --verbose ./tests/repositories",
    "test:unit:services": "jest --coverage --verbose ./tests/services"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "jest": "^27.4.3",
    "sinon": "^12.0.1",
    "supertest": "^6.1.6"
  },
  "dependencies": {
    "bcrypt": "^5.0.1",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^6.1.2",
    "validator": "^13.7.0"
  },
  "jest": {
    "setupFiles": [
      "<rootDir>/jest/.env.jest.js"
    ]
  }
}
